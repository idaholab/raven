#!/bin/bash

# activate conda environement
. scripts/establish_conda_env.sh --load

echo DEBUGG python: `which python`

# set up hit.cpp so that it has a newer timestamp than hit.pyx
touch moose/framework/contrib/hit/hit.cpp

# make
make
